---
pipeline: my-cli
global:
    workdir: my_dir/here
    env: [CI=true]

tasks:
    - name: Run docker build # optional.
      idontexist: docker::build # each built-in action is separated by ::
      with:
          - docker build -t my-image .
          - docker run my-image
#   env: ["CI=true" ]
